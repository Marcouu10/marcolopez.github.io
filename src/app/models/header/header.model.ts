export class Header {
    id?: string;
    name?: string;
    description?: string;
    email?: string;
    phoneNumber?: string;
    location?: string;
    // Usamos 'photo_url' para que coincida con el espacio en tu captura de Firestore
    "photo url"?: string; 
    GitHub?: string;
    Linkedin?: string;
}
